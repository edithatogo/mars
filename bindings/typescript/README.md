# @mars-earth/runtime

Portable JavaScript runtime replay for mars `ModelSpec` artifacts.

This package loads validated model specifications and evaluates design matrices
and predictions in JavaScript. It is part of the binding-first roadmap: language
surfaces share the same fixture contract before the training core migrates to
Rust.

## Validation

```sh
npm test
```
