# Tasks

Current priorities:

- Preserve the Python estimator APIs as the compatibility baseline
- Maintain the portable `ModelSpec` contract and fixture corpus
- Grow the Rust replay prototype into the shared Rust core
- Keep Rust runtime parity passing against checked-in portable fixtures
- Define binding boundaries for Python, R, Julia, Rust, C#, Go, and TypeScript
- Keep weighted forward/pruning selection covered by regression tests as the core runtime evolves
- Reduce sklearn expected-failure gaps only where they match the documented contract
- Add shared conformance tests that every language binding must pass
