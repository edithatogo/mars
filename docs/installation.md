# Installation

## PyPI

```bash
pip install mars-earth
```

## Development

```bash
uv sync --extra dev
uv run pytest -q
```

## Package naming

- Project brand: `mars`
- Python import: `pymars`
- PyPI distribution: `mars-earth`
