# Requirements

Core requirements remain unchanged:

- preserve the `pymars` import style
- remain sklearn compatible
- keep the implementation pure Python at the public surface
- use numpy and SciPy-class dependencies only where needed
- maintain portable `ModelSpec` compatibility
