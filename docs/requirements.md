# Requirements

## Runtime

- Python 3.9+
- NumPy
- scikit-learn
- matplotlib

## Development

Use the `dev` extra in `pyproject.toml` to install the test, lint, benchmark, and documentation toolchain.
