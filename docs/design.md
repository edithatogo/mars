# Design

The design remains focused on a Python-first public API with a shared portable
runtime core.

The current emphasis is:

- sklearn-compatible estimators
- portable `ModelSpec` replay
- Rust-backed shared runtime behavior
- conservative release and CI policy
