# Remaining Roadmap

The remaining roadmap is organized around the open Conductor tracks.

The active priorities are:

- finish Rust training parity
- keep CI and supply-chain checks aligned with the manifests
- complete R package publication readiness before R publication
- complete publication handoff for the remaining registries
- preserve the current `pymars` import API while the public package family
  stays under `mars-earth`
