# Model Spec

Portable `ModelSpec` artifacts are the shared runtime contract between
language bindings.

The current implementation supports:

- validation
- `design_matrix`
- `predict`
- export and import of portable specs

See [Rust Core](rust_core.md) and [Binding ABI and API Contract](binding_abi_contract.md)
for the runtime boundary.
