# Plan: Starlight Docs Migration

**Track:** `starlight_migration_20260513`
**Requirement:** REQ-MIG-003

## Steps Executed

### Step 1: Create Starlight Scaffold

- Created `docs/astro-site/` directory structure
- Created `package.json` with all required dependencies:
  - `@astrojs/starlight ^0.39.0`
  - `@astrojs/sitemap ^3.3.0`
  - `starlight-polyglot` (file link to local package)
  - `starlight-links-validator ^0.19.2`
  - `starlight-versions ^0.8.0`
  - `starlight-llms-txt ^1.0.0`
- Created `tsconfig.json` extending `astro/tsconfigs/strict`
- Created `public/.nojekyll` for GitHub Pages deployment
- Created `astro.config.mjs` with:
  - Starlight integration with social links, edit URL
  - polyglot plugin configured for Python only (`entryPoints: ['pymars']`)
  - Sidebar with sections: Home, Installation, Usage, Tutorials,
    API Reference (autogenerated via polyglot), Bindings, HPC,
    Contributor Guides, Architecture

### Step 2: Convert MkDocs Content to MDX

- Ran automated conversion script to:
  - Convert all 74 `.md` files to `.mdx` with frontmatter
  - Copy 7 `.ipynb` notebook files as-is
  - Created `release_metadata.mdx` page embedding JSON content
- Output: `docs/astro-site/src/content/docs/`

### Step 3: Update CI/CD

- Modified `.github/workflows/docs.yml`:
  - Replaced `uv`/Python/MkDocs build with `pnpm`/Node.js/Astro build
  - Added `pnpm/action-setup` for pnpm installation
  - Working directory set to `docs/astro-site`
  - Output remains `site/` for GH Pages deployment

### Step 4: Update Governance Documents

- Updated `conductor/tech-stack.md`:
  - Removed `mkdocs` reference
  - Marked Starlight as ACTIVE (was "planned")
  - Added polyglot, links-validator, versions, llms-txt entries
- Updated `docs/starlight_docs_stack.md`:
  - Marked as "Migration completed 2026-05-13"
  - Documented adopted stack and validation requirements
  - Added historical section for previous MkDocs stack

### Step 5: Create Conductor Track

- Created `conductor/tracks/starlight_migration_20260513/` with:
  - `metadata.json` — track metadata (completed status)
  - `spec.md` — fulfills REQ-MIG-003
  - `plan.md` — this file

## Post-Migration Checklist

- [x] Starlight scaffold created at `docs/astro-site/`
- [x] All MkDocs MD files converted to MDX with frontmatter
- [x] Sidebar configured matching MkDocs nav structure
- [x] `.github/workflows/docs.yml` updated to Astro build
- [x] `conductor/tech-stack.md` updated (Starlight ACTIVE)
- [x] `docs/starlight_docs_stack.md` updated (completed)
- [x] Conductor track created with spec + plan
- [ ] Verify `pnpm install` resolves all dependencies
- [ ] Verify `pnpm run build` succeeds
- [ ] Verify GH Pages deployment works
- [ ] Remove `mkdocs.yml` once migration is confirmed stable
